import { Progress as NextUIProgress } from '@nextui-org/react';

/**
 * Progress component wrapper for NextUI Progress
 * Preserves ocean theme styling while using NextUI components
 */
export const Progress = NextUIProgress;

export default Progress;
