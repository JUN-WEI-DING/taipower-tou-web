import { Card as NextUICard } from '@nextui-org/react';

/**
 * Card component wrapper for NextUI Card
 * Preserves ocean theme styling while using NextUI components
 */
export const Card = NextUICard;
export { CardHeader, CardBody, CardFooter } from '@nextui-org/react';

export default Card;
