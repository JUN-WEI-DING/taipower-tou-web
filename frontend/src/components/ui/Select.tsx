import { Select as NextUISelect } from '@nextui-org/react';

/**
 * Select component wrapper for NextUI Select
 * Preserves ocean theme styling while using NextUI components
 */
export const Select = NextUISelect;

export default Select;
