import { Input as NextUIInput } from '@nextui-org/react';

/**
 * Input component wrapper for NextUI Input
 * Preserves ocean theme styling while using NextUI components
 */
export const Input = NextUIInput;

export default Input;
