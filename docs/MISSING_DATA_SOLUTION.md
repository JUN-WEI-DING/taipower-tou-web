# 電費單資料不完整問題 - 完整解決方案

## 所有使用情境總覽

| 你的電費單型別 | 電費單有什麼 | 想比較 | 需要什麼 | 結果 |
|--------------|-------------|--------|---------|------|
| **非時間電價** | 總用電 350度 | 兩段式 | 尖峰+離峰 | ⚠️ 需估算 |
| **非時間電價** | 總用電 350度 | 三段式 | 尖峰+半尖+離峰 | ⚠️ 需估算 |
| **兩段式時間電價** | 尖峰120 + 離峰230 | 三段式 | 尖峰+半尖+離峰 | ⚠️ 需拆分 |
| **三段式時間電價** | 完整資料 | 任何 | - | ✅ 都準確 |

---

## 情境 A：非時間電價電費單（最常見）

### 電費單長這樣

```
┌─────────────────────────────────────┐
│ 臺灣電力公司 電費通知單             │
├─────────────────────────────────────┤
│ 計費期間：114年01月                 │
│ 本期用電：350 度                    │
│ 電費小計：$2,345                    │
└─────────────────────────────────────┘
```

### 問題

只有總用電，不知道「白天用多少」「晚上用多少」

### 解決方式：選擇用電習慣

```
選一個最像你家的用電習慣

┌─────────────────────────────────────────────┐
│ 👨‍💼👩‍💼 一般上班族家庭     [最常見]         │
│ ─────────────────────────────────────────   │
│ 白天出門工作，晚上回家才用電                │
│                                             │
│ 典型的一天：                                │
│ • 早上 7-9 點：趕快出門，用電不多           │
│ • 白天 9-5 點：家裡沒人，只有冰箱           │
│ • 晚上 6-11 點：回家，開燈電視煮飯冷氣      │
│ • 深夜 11 點後：大家都睡了                  │
│                                             │
│ 預估分配：                                  │
│ 🌆 傍晚晚間 35%  ☀️ 白天 25%  🌙 深夜 40%  │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ 👵👶 白天在家的家庭                         │
│ ─────────────────────────────────────────   │
│ 長輩或小孩白天都在家                        │
│                                             │
│ 典型的一天：                                │
│ • 早上 8-11 點：在家，煮飯洗衣看電視        │
│ • 白天 11-5 點：持續在家，冷氣開比較久      │
│ • 晚上 6-11 點：大家都在家用電              │
│                                             │
│ 預估分配：                                  │
│ 🌆 傍晚晚間 45%  ☀️ 白天 20%  🌙 深夜 35%  │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ 🦉 夜貓子型                                 │
│ ─────────────────────────────────────────   │
│ 深夜還在活動，半夜洗衣曬衣                  │
│                                             │
│ 典型的一天：                                │
│ • 早上 9-11 點才起床，早上用電少            │
│ • 白天可能外出或午睡                        │
│ • 晚上 11 點後還沒睡，洗衣機這時候跑        │
│                                             │
│ 預估分配：                                  │
│ 🌆 傍晚晚間 25%  ☀️ 白天 20%  🌙 深夜 55%  │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ 🎚️ 我自己調整                               │
│ ─────────────────────────────────────────   │
│ 你知道自己的用電習慣？用滑桿自己決定！      │
└─────────────────────────────────────────────┘
```

---

## 情境 B：兩段式時間電價電費單

### 電費單長這樣

```
┌─────────────────────────────────────┐
│ 臺灣電力公司 電費通知單             │
│ 簡易型時間電價-二段式               │
├─────────────────────────────────────┤
│ 計費期間：114年01月                 │
│                                     │
│ 尖峰用電：120 度                    │
│ 離峰用電：230 度                    │
│ ────────────────────────────        │
│ 總用電：350 度                      │
│ 電費小計：$2,180                    │
└─────────────────────────────────────┘
```

### 資料完整度

```
✅ 非時間電價：可準確計算
✅ 兩段式時間電價：可準確計算（當前方案）
⚠️ 三段式時間電價：需要處理（見下文）
```

### 三段式的問題

```
你的電費單有：尖峰 + 離峰
三段式需要：尖峰 + 半尖峰 + 離峰
                    ↑
                 少了這個！
```

### 為什麼會少？時段定義不同

```
兩段式的「尖峰」時段 = 三段式的「尖峰」+「半尖峰的一部分」

兩段式：        尖峰：09:00 ~ 24:00
               離峰：00:00 ~ 09:00

三段式：        尖峰：     10:00~12:00, 16:00~21:00  (7小時)
               半尖峰：  07:00~10:00, 12:00~16:00, 21:00~23:00  (9小時)
               離峰：    23:00~07:00  (8小時)
               ────────────────────────────────────────
               總計：24小時

兩段式尖峰(15小時) = 三段式尖峰(7小時) + 三段式半尖峰(部分)
                     7/15 = 46.7%是尖峰，9/15 = 53.3%是半尖峰
```

### 解決方式：選擇拆分方式

```typescript
// UI 顯示
┌─────────────────────────────────────────────────────────────┐
│  你的電費單有完整的兩段式資料                                │
│                                                             │
│  ✅ 非時間電價：可準確計算                                  │
│  ✅ 兩段式時間電價：可準確計算  ← 你的當前方案              │
│  ⚠️ 三段式時間電價：需要處理                                │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│  三段式需要半尖峰資料，我們可以幫你從尖峰拆分：            │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  兩段式的尖峰用電 120 度 = 三段式的尖峰 + 半尖峰(部分)      │
│                                                             │
│  [使用預設拆分比例]  推薦                                    │
│      依時段長度比例拆分                                     │
│      → 尖峰 56 度 (46.7%) + 半尖峰 64 度 (53.3%)           │
│      再加上離峰時段的半尖峰用電（從離峰估算）               │
│                                                             │
│  [保守拆分]                                                  │
│      假設尖峰時段用電較多                                   │
│      → 尖峰 72 度 (60%) + 半尖峰 48 度 (40%)               │
│                                                             │
│  [積極拆分]                                                  │
│      假設半尖峰時段用電較多                                 │
│      → 尖峰 36 度 (30%) + 半尖峰 84 度 (70%)               │
│                                                             │
│  [我自己決定比例]                                            │
│      用滑桿決定拆分比例                                     │
│      尖峰：████░░░░ 60%                                    │
│      半尖峰：███████ 40%                                    │
└─────────────────────────────────────────────────────────────┘
```

### 結果顯示

```
🥇 三段式時間電價    $2,150  ⚠️部分估算  從尖峰拆分
🥈 兩段式時間電價    $2,180  ✅準確      當前方案
 3  住家用電(非時間)  $2,345  ✅準確

💡 三段式比兩段式省 $30（從尖峰按比例拆分半尖峰估算）
   實際省多少要看你的用電時間分佈
```

---

## 情境 C：三段式時間電價電費單（最完整）

### 電費單長這樣

```
┌─────────────────────────────────────┐
│ 臺灣電力公司 電費通知單             │
│ 簡易型時間電價-三段式               │
├─────────────────────────────────────┤
│ 計費期間：114年01月                 │
│                                     │
│ 尖峰用電：70 度                     │
│ 半尖峰用電：80 度                   │
│ 離峰用電：200 度                    │
│ ────────────────────────────        │
│ 總用電：350 度                      │
│ 電費小計：$2,100                    │
└─────────────────────────────────────┘
```

### 資料完整度

```
✅ 所有方案都可以準確計算！

非時間電價：用總用電計算
兩段式時間電價：半尖峰併入尖峰計算
三段式時間電價：完整資料計算
```

### 結果顯示

```
🥇 三段式時間電價    $2,100  ✅準確  當前方案
🥈 兩段式時間電價    $2,180  ✅準確
 3  住家用電(非時間)  $2,345  ✅準確

所有結果都是依據電費單上的實際用電資料計算
```

---

## 統一的結果標示系統

| 標示 | 意義 | 什麼時候出現 |
|------|------|-------------|
| **✅ 準確** | 完全依據電費單資料 | 資料足夠計算該方案 |
| **⚠️ 需估算** | 需選擇用電習慣 | 非時間電價 → 時間電價 |
| **⚠️ 部分估算** | 需拆分時段 | 兩段式 → 三段式 |

### 結果卡片設計

```typescript
// components/results/PlanCard.tsx

function PlanCard({ result, label }: PlanCardProps) {
  return (
    <Card>
      <div className="flex items-center justify-between">
        <h4>{result.planName}</h4>

        {/* 準確度標籤 */}
        <Badge variant={label.accuracy === 'accurate' ? 'success' : 'warning'}>
          {label.badge}
        </Badge>
      </div>

      <div className="text-2xl font-bold">
        ${result.charges.total}
      </div>

      {/* 說明 */}
      {label.accuracy !== 'accurate' && (
        <p className="text-sm text-gray-600">
          {label.tooltip}
        </p>
      )}

      {/* 與當前方案比較 */}
      {result.comparison.difference < 0 && (
        <p className="text-green-600">
          省 ${Math.abs(result.comparison.difference)}
        </p>
      )}
    </Card>
  );
}
```

---

## 完整判斷流程圖

```
                    電費單上傳
                        │
                        ▼
              ┌─────────────────┐
              │   OCR 識別      │
              └────────┬────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │  判斷電費單型別與資料完整度  │
        └──────────────┬───────────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
        ▼              ▼              ▼
   ┌─────────┐   ┌─────────┐   ┌─────────┐
   │非時間電價│   │兩段式   │   │三段式   │
   │只有總用電│   │尖峰+離峰│   │完整資料 │
   └────┬────┘   └────┬────┘   └────┬────┘
        │              │              │
        ▼              ▼              ▼
 ┌──────────┐   ┌──────────┐   ┌──────────┐
 │選擇用電  │   │比較三段式│   │所有方案  │
 │習慣估算  │   │選擇拆分  │   │準確計算  │
 └─────┬────┘   └─────┬────┘   └─────┬────┘
       │              │              │
       ▼              ▼              ▼
 ┌──────────┐   ┌──────────┐   ┌──────────┐
 │所有方案  │   │所有方案  │   │所有方案  │
 │標示估算  │   │部分準確  │   │全部準確  │
 └──────────┘   └──────────┘   └──────────┘
```

---

## 技術實作重點

### 1. 資料完整度偵測

```typescript
// services/data/DataCompletenessDetector.ts

class DataCompletenessDetector {
  /**
   * 偵測電費單資料完整度
   */
  static detect(parsedBill: ParsedBill): {
    level: DataCompletenessLevel;
    missingData: string[];
    canCalculateAccurately: string[];
    needsEstimation: string[];
    needsSplit: string[];
  } {
    const hasTotal = !!parsedBill.consumption.total;
    const hasPeak = parsedBill.consumption.peakOnPeak !== undefined;
    const hasSemiPeak = parsedBill.consumption.semiPeak !== undefined;
    const hasOffPeak = parsedBill.consumption.offPeak !== undefined;

    // 判斷完整度等級
    let level: DataCompletenessLevel;
    if (hasTotal && !hasPeak && !hasSemiPeak && !hasOffPeak) {
      level = DataCompletenessLevel.TOTAL_ONLY;
    } else if (hasTotal && hasPeak && hasOffPeak && !hasSemiPeak) {
      level = DataCompletenessLevel.TWO_TIER;
    } else if (hasTotal && hasPeak && hasSemiPeak && hasOffPeak) {
      level = DataCompletenessLevel.THREE_TIER;
    } else {
      level = DataCompletenessLevel.INCOMPLETE;
    }

    // 分析可準確計算的方案
    const canCalculateAccurately: string[] = [];
    const needsEstimation: string[] = [];
    const needsSplit: string[] = [];

    switch (level) {
      case DataCompletenessLevel.TOTAL_ONLY:
        canCalculateAccurately.push('non_tou');
        needsEstimation.push('two_tier', 'three_tier');
        break;

      case DataCompletenessLevel.TWO_TIER:
        canCalculateAccurately.push('non_tou', 'two_tier');
        needsSplit.push('three_tier');
        break;

      case DataCompletenessLevel.THREE_TIER:
        canCalculateAccurately.push('non_tou', 'two_tier', 'three_tier');
        break;
    }

    return {
      level,
      missingData: [],
      canCalculateAccurately,
      needsEstimation,
      needsSplit,
    };
  }
}
```

### 2. 從兩段式拆分到三段式

```typescript
// services/calculation/TwoTierSplitter.ts

class TwoTierSplitter {
  /**
   * 將兩段式資料拆分為三段式
   * @param peakOnPeak 兩段式的尖峰用電
   * @param offPeak 兩段式的離峰用電
   * @param splitMode 拆分模式
   */
  static split(
    peakOnPeak: number,
    offPeak: number,
    splitMode: 'default' | 'conservative' | 'aggressive' | 'custom',
    customPercent?: number // 尖峰佔兩段式尖峰的百分比
  ): TOUConsumption {
    let peakPercent: number;

    switch (splitMode) {
      case 'default':
        // 依時段長度比例：尖峰7小時 / 尖峰15小時 = 46.7%
        peakPercent = 7 / 15;
        break;

      case 'conservative':
        // 保守：假設尖峰時段用電較多
        peakPercent = 0.6;
        break;

      case 'aggressive':
        // 積極：假設半尖峰時段用電較多
        peakPercent = 0.3;
        break;

      case 'custom':
        peakPercent = (customPercent || 50) / 100;
        break;
    }

    // 兩段式尖峰拆分
    const threeTierPeak = peakOnPeak * peakPercent;
    const threeTierSemiPeakFromPeak = peakOnPeak * (1 - peakPercent);

    // 兩段式離峰中，三段式半尖峰時段的用電
    // 三段式半尖峰在兩段式離峰時段：23:00-01:00 (2小時)
    // 兩段式離峰總共：00:00-09:00 (9小時)
    // 比例：2/9 = 22.2%
    const threeTierSemiPeakFromOffPeak = offPeak * (2 / 9);

    // 三段式離峰
    const threeTierOffPeak = offPeak * (7 / 9); // 剩餘部分

    return {
      peakOnPeak: threeTierPeak,
      semiPeak: threeTierSemiPeakFromPeak + threeTierSemiPeakFromOffPeak,
      offPeak: threeTierOffPeak,
    };
  }

  /**
   * 取得預設拆分比例的說明
   */
  static getDefaultSplitExplanation(): string {
    return `
      兩段式尖峰時段 (09:00-24:00，共15小時)
      = 三段式尖峰 (10:00-12:00, 16:00-21:00，共7小時)
      + 三段式半尖峰的一部分

      按時段長度比例：7小時 ÷ 15小時 = 46.7%
    `;
  }
}
```

### 3. 結果標籤系統

```typescript
// services/calculation/ResultLabeler.ts

interface ResultLabel {
  accuracy: 'accurate' | 'estimated' | 'partial_estimated';
  badge: string;
  tooltip: string;
  detail?: string;
}

class ResultLabeler {
  static label(
    targetPlanType: 'non_tou' | 'two_tier' | 'three_tier',
    dataLevel: DataCompletenessLevel,
    estimationMode?: EstimationMode
  ): ResultLabel {
    // 完整資料：所有都準確
    if (dataLevel === DataCompletenessLevel.THREE_TIER) {
      return {
        accuracy: 'accurate',
        badge: '✅ 準確',
        tooltip: '依據電費單資料計算',
      };
    }

    // 兩段式資料
    if (dataLevel === DataCompletenessLevel.TWO_TIER) {
      if (targetPlanType === 'three_tier') {
        return {
          accuracy: 'partial_estimated',
          badge: '⚠️ 部分估算',
          tooltip: '從兩段式尖峰按比例拆分半尖峰',
          detail: '兩段式尖峰時段包含三段式的尖峰和部分半尖峰',
        };
      }
      return {
        accuracy: 'accurate',
        badge: '✅ 準確',
        tooltip: '依據電費單資料計算',
      };
    }

    // 只有總用電
    if (dataLevel === DataCompletenessLevel.TOTAL_ONLY) {
      if (targetPlanType === 'non_tou') {
        return {
          accuracy: 'accurate',
          badge: '✅ 準確',
          tooltip: '依據電費單資料計算',
        };
      }

      // 時間電價需要估算
      const habitLabels = {
        [EstimationMode.AVERAGE]: '一般上班族家庭',
        [EstimationMode.HOME_DURING_DAY]: '白天在家的家庭',
        [EstimationMode.NIGHT_OWL]: '夜貓子型',
        [EstimationMode.CUSTOM]: '自訂比例',
      };

      return {
        accuracy: 'estimated',
        badge: '⚠️ 估算',
        tooltip: `依「${habitLabels[estimationMode!]}」估算`,
        detail: '實際電費可能因用電習慣不同而有所差異',
      };
    }

    return {
      accuracy: 'accurate',
      badge: '✅ 準確',
      tooltip: '依據電費單資料計算',
    };
  }
}
```

---

## UI 元件設計

### 1. 資料完整度顯示

```tsx
// components/data/DataCompletenessBanner.tsx

function DataCompletenessBanner({
  detection,
  currentPlanType,
}: DataCompletenessBannerProps) {
  return (
    <Card className="bg-blue-50 border-blue-200">
      <CardContent className="p-4">
        <h4 className="font-bold flex items-center gap-2">
          <span>📊</span>
          資料完整度分析
        </h4>

        <div className="mt-3 space-y-2">
          {/* 當前方案 */}
          <div className="flex items-center gap-2">
            <Badge variant="success">✅ 準確</Badge>
            <span>當前方案：{getPlanTypeName(currentPlanType)}</span>
          </div>

          {/* 可準確計算 */}
          {detection.canCalculateAccurately.length > 0 && (
            <div className="text-sm">
              <span className="text-green-700">✅ 可準確計算：</span>
              {detection.canCalculateAccurately.map(getPlanTypeName).join('、')}
            </div>
          )}

          {/* 需要估算 */}
          {detection.needsEstimation.length > 0 && (
            <div className="text-sm">
              <span className="text-amber-700">⚠️ 需估算：</span>
              {detection.needsEstimation.map(getPlanTypeName).join('、')}
            </div>
          )}

          {/* 需要拆分 */}
          {detection.needsSplit.length > 0 && (
            <div className="text-sm">
              <span className="text-orange-700">🔪 需拆分：</span>
              {detection.needsSplit.map(getPlanTypeName).join('、')}
            </div>
          )}
        </div>
      </CardContent>
    </Card>
  );
}
```

### 2. 拆分模式選擇器

```tsx
// components/split/SplitModeSelector.tsx

function SplitModeSelector({
  peakOnPeak,
  offPeak,
  onModeSelected,
}: SplitModeSelectorProps) {
  const modes = [
    {
      id: 'default',
      title: '預設拆分（推薦）',
      description: '依時段長度比例拆分',
      calculation: TwoTierSplitter.split(peakOnPeak, offPeak, 'default'),
      explanation: '兩段式尖峰時段 = 三段式尖峰(46.7%) + 三段式半尖峰(53.3%)',
    },
    {
      id: 'conservative',
      title: '保守拆分',
      description: '假設尖峰時段用電較多',
      calculation: TwoTierSplitter.split(peakOnPeak, offPeak, 'conservative'),
      explanation: '尖峰時段用電：60% 尖峰 + 40% 半尖峰',
    },
    {
      id: 'aggressive',
      title: '積極拆分',
      description: '假設半尖峰時段用電較多',
      calculation: TwoTierSplitter.split(peakOnPeak, offPeak, 'aggressive'),
      explanation: '尖峰時段用電：30% 尖峰 + 70% 半尖峰',
    },
  ];

  return (
    <div className="space-y-4">
      <h4 className="font-bold">選擇拆分方式</h4>
      <p className="text-sm text-gray-600">
        兩段式的尖峰用電 {peakOnPeak} 度需要拆分為三段式的尖峰和半尖峰
      </p>

      <RadioGroup value={selectedMode} onValueChange={setSelectedMode}>
        {modes.map((mode) => (
          <div key={mode.id} className="border rounded p-4">
            <div className="flex items-start gap-3">
              <RadioGroupItem value={mode.id} id={mode.id} />
              <div className="flex-1">
                <label htmlFor={mode.id} className="font-medium cursor-pointer">
                  {mode.title}
                </label>
                <p className="text-sm text-gray-600">{mode.description}</p>
                <p className="text-xs text-gray-500 mt-1">{mode.explanation}</p>
                <div className="mt-2 text-sm">
                  <span className="text-red-600">尖峰 {mode.calculation.peakOnPeak.toFixed(0)}度</span>
                  {' + '}
                  <span className="text-yellow-600">半尖峰 {mode.calculation.semiPeak.toFixed(0)}度</span>
                  {' + '}
                  <span className="text-green-600">離峰 {mode.calculation.offPeak.toFixed(0)}度</span>
                </div>
              </div>
            </div>
          </div>
        ))}
      </RadioGroup>

      <Button onClick={() => onModeSelected(selectedMode)}>
        使用此拆分方式
      </Button>
    </div>
  );
}
```

---

## 總結

這份完整解決方案涵蓋了三種主要情境：

| 情境 | 電費單型別 | 需要什麼處理 |
|------|-----------|-------------|
| **A** | 非時間電價（只有總用電） | 選擇用電習慣估算時段 |
| **B** | 兩段式時間電價（尖峰+離峰） | 選擇拆分方式處理三段式 |
| **C** | 三段式時間電價（完整資料） | 無需處理，全部準確 |

每種情境都有：
1. 清楚的問題說明
2. 接地氣的解決方案
3. 視覺化的選擇介面
4. 明確的結果標示
